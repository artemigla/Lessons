import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    title: {
        marginTop: 10,
        alignItems: 'center'
    },
    emptyList: {
        color: '#010101',
        fontSize: 18,
        marginTop: '35%',
        alignItems: 'center'
    },
});